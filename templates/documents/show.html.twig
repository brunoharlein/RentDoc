{% extends 'base.html.twig' %}

{% block title %}Documents{% endblock %}

{% block body %}
    <h1>Documents</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ document.title }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ document.author }}</td>
            </tr>
            <tr>
                <th>Edition</th>
                <td>{{ document.releaseDate ? document.releaseDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Resume</th>
                <td>{{ document.resume }}</td>
            </tr>
            <tr>
                <th>categorie</th>
                <td>{{ document.category.name }}</td>
            </tr>
            <tr>
                <th>Emprunteur</th>
                <td>{{ document.borrower }}</td>
            </tr>
            {# <tr>
              <th>Emprunteur</th>
              <td>{{ document.borrower.name }}</td> 
            </tr> #}
        </tbody>
    </table>

    <a href="{{ path('documents_index') }}" class="btn btn-success">Revenir à la liste/</a>
    <a href="{{ path('documents_rent', {'id': document.id}) }}" class="btn btn-success">Emprunter</a>
    <a href="{{ path('documents_edit', {'id': document.id}) }}" class="btn btn-success">Editer</a>
    {{ include('documents/_delete_form.html.twig') }}
{% endblock %}
