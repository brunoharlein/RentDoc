{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_index') }}">Revenir à la liste</a>

    <a href="{{ path('category_edit', {'id': category.id}) }}">Editer</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
